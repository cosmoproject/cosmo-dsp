# cosmo-dsp

The COSMO DSP library contains ready-made csound audio effects (DSP-Library/Effects) and the COSMO-Patcher, a tool to easily use the effects with hardware controllers like the COSMO hardware boxes or a MIDI device.
